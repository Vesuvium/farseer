defmodule Farseer.Handlers.S3 do
  @moduledoc """
  The S3 handler can be used to interact with AWS S3-compliant services (S3 itself, Digitalocean spaces and so on). 
  """
  def handle(_conn, _options) do
    :ok
  end
end
